{
  "name": "api-bancaire-javascript-sharifatou",
  "version": "1.0.0",
  "description": "API bancaire simple en Node.js sans dependances externes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js 8006",
    "test": "vitest run",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "test:coverage": "vitest run --coverage",
    "test:performance:hey": "./test/performance/hey/run-hey.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "supertest": "^7.1.4",
    "vitest": "^4.0.3"
  }
}
